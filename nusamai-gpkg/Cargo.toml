[package]
name = "nusamai-gpkg"
version = "0.1.0"
edition = "2021"

[dependencies]
sqlx = { version = "0.8.2", features = ["sqlite", "runtime-tokio"] }
flatgeom = "0.0"
thiserror = "1.0.65"
url = "2.5.2"
indexmap = "2.6.0"

[dev-dependencies]
tokio = { version = "1.41", features = ["full"] }
